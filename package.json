{
  "name": "nutri-tracker",
  "version": "1.0.0",
  "description": "Premium offline-capable calorie & macro tracker",
  "main": "app.js",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^0.34.6",
    "jsdom": "^22.1.0"
  }
}
